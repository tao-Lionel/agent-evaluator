agent:
  adapter: http_bot
  bot_url: http://127.0.0.1:8000/chat
  message_field: message
  reply_field: reply
  timeout: 60

environment:
  type: passthrough

evaluators:
  - info_delivery
  - llm_judge

scenarios: scenarios/http_bot_tasks.json

run:
  num_trials: 1
  log_level: INFO
